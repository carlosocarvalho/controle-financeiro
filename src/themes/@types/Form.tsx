export default interface Form {
  input: {
    input: {
      borderColor: string;
      color: string;
      paddingVertical: string;
    };
    label: {
      borderColor: string;
      color: string;
    };
  };
}
