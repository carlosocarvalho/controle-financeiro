export default interface Form {
  input: {
    borderColor: string;
    color: string;
    paddingVertical: string;
    height: string;
  };
  label: {
    borderColor: string;
    color: string;
  };
}
